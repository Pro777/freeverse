{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "predev": "node scripts/build-poem-index.mjs && node scripts/build-search-index.mjs && node scripts/build-public-alcove-index.mjs",
    "dev": "astro dev",
    "prebuild": "node scripts/build-poem-index.mjs && node scripts/build-search-index.mjs && node scripts/build-public-alcove-index.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "vitest run",
    "test:unit:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "@huggingface/transformers": "^4.0.1",
    "@tailwindcss/vite": "^4.2.2",
    "astro": "^6.1.10",
    "js-yaml": "^4.1.1",
    "minisearch": "^7.2.0",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "@vitest/coverage-v8": "^4.0.18",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=20"
  }
}
